.delimiter {
    border-bottom-color: var(--unity-colors-separator-background);
    border-bottom-width: 1px;
    padding-bottom: 10px;
}

.hor-line {
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
}

.search-popup__info-box {
    height: 22px;
    border-bottom-width: 1px;
    border-bottom-color: var(--unity-colors-separator-background);
}
